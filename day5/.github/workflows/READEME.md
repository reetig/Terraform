# Day 5 - Terraform CI Workflow

This assignment demonstrates infrastructure validation using GitHub Actions.

Workflow runs automatically when code is pushed.

Commands executed in CI:

terraform fmt
terraform init
terraform validate
terraform plan